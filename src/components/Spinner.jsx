import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      {/* Insert your spinner content or styling here */}
      Loading...
    </div>
  );
};

export default Spinner;
